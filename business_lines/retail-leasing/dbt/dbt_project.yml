# business_lines/retail-leasing/dbt/dbt_project.yml
# 零售租赁与市场报告 DBT 项目.
# 单一 mart fct_retail_leasing 直接给应用层消费.
name: 'retail_leasing'
version: '1.0.0'
config-version: 2

profile: 'retail_leasing'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"

models:
  retail_leasing:
    marts:
      +materialized: table
      +schema: mart
